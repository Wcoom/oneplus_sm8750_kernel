# 2026-09 ACK 维护报告

## 维护目标

- 分支：`6.6.118-13T`
- 回退基线：`941a8c58f2f8d1093e1ec722057e55cbe960aa11`
- ACK 源：官方 `https://android.googlesource.com/kernel/common`，分支 `android15-6.6`
- 合并方式：以回退基线为第一父提交、官方 ACK HEAD 为第二父提交的双父合并

## ACK 合并结果

- 共同基线：`5ef17cb58b6e6ede5281d9950baf0738c9c5f14d`
- 官方最新提交：`d645d30475a90d74210e3afe85e9a6ba748019b3`
- 最新提交主题：`ANDROID: certs: extract-cert: Guard key_pass use with USE_PKCS11_ENGINE`
- 增量数量：21 个提交
- 合并提交：`574270a7d4613899504118109097bf9531c6bb98`
- 冲突级别：L0，零冲突；未使用 `-X ours` 或 `-X theirs`
- 主要 ACK 变更：AFDO 元数据、GKI symbol list、HID/NFC/IPv6 安全修复、fuse-bpf/f2fs 修复、wrapfd 异步 I/O 与 DMA-BUF 映射改进、KVM/pKVM 修复、GIC-v3 vendor hook 恢复、extract-cert PKCS#11 条件保护。

## ABI/KMI 与定制红线

- `include/linux/sched.h` 的 `ANDROID_KABI_USE(6, struct sysv_sem sysvsem)` 和 `_ANDROID_KABI_REPLACE(ANDROID_KABI_RESERVE(7); ANDROID_KABI_RESERVE(8), struct sysv_shm sysvshm)` 保留。
- `kernel/pid.c` 的 `ghost_task` 引用计数为 12。
- `android/abi_gki_aarch64.stg` 相对回退基线仅新增 16 行，无删除。
- `include/trace/hooks` 与 `drivers/android/vendor_hooks.c` 无本地改删；ACK 钩子变更保持上游内容。
- `SUBLEVEL = 118` 保持。
- `CONFIG_ZRAM=n` 保持；`CONFIG_CRYSTAL_HYBRIDSWAP=y`、`CONFIG_NTSYNC=y`、`CONFIG_REKERNEL_X=y`、`CONFIG_NET_SCH_FQ_GUARD=y`、`CONFIG_BBG=y` 及命名空间/SYSVIPC 配置均在生成的 `.config` 中生效。
- 目标提交的 BBG gitlink `6e32d811ef5072c5454577915c118db5ba2b5c15` 已从本地 bundle 精确恢复，未改写主仓库 gitlink。
- `git diff --check` 通过，冲突标记扫描通过。

## 编译与产物

- 构建命令：`bash /home/wcoom/内核构建.sh`
- 构建结果：通过
- 版本：`6.6.118-android15-8-gf4dc45704e54-abogki20260727-4k`
- Image：39,262,720 字节
- Image SHA-256：`26777f41d73a0fd9fc3e6e15c40c34d71575478ca1acba70355f833667ea5549`
- `ntsync` 符号计数：97
- AnyKernel3：`AnyKernel3-20260909-2343.zip`
- ZIP 大小：32,162,700 字节
- ZIP SHA-256：`d421eb9face9c43463ed09d784a0c074d208d083ccc509425c50ee442dce141f`
- 已验证 ZIP 内 `Image` 的 SHA-256 与构建输出一致。

## 2026-09-20 第八轮 ACK 增量

- ACK 区间：`d645d30475a90d74210e3afe85e9a6ba748019b3` → `448c303366032107c46d39006c8127a5ca967a26`。
- 更新数量：11 个官方提交，包含 fuse-bpf/wrapfd 安全修复、Bluetooth UAF/边界修复、KVM、wakeup_reason、posix-cpu-timers、arm64 no32bit_el0 与 GKI symbol list 更新。
- 合并提交：`3ab6beb853043`；备份分支：`backup/pre-ack-20260920`。

## 冲突处理记录

- L1：`android/abi_gki_aarch64_sunxi`。本地已有的 ABI 符号全部保留，在其后追加 ACK 新增的 11 个 console/Type-C/USB-PD 符号；未使用 `-X ours` 或 `-X theirs`。
- 无 L2/L3 冲突；`git diff --name-only --diff-filter=U` 与 `git ls-files -u` 均为空。

## 红线与构建

- ABI/KMI：PASS；`sched.h` Droidspaces KABI 槽位保留，`kernel/pid.c` 的 `ghost_task` 12 行，NTSYNC 97 个符号，vendor hook 无本地改删。
- 定制配置：PASS；`CONFIG_SYSVIPC`、`POSIX_MQUEUE`、`IPC_NS`、`USER_NS`、`PID_NS`、`NTSYNC`、`REKERNEL_X`、`NET_SCH_FQ_GUARD`、`BBG` 均为 `y`，`CONFIG_ZRAM=n`，`SUBLEVEL=118`。
- 构建：`bash /home/wcoom/内核构建.sh` exit 0；版本 `6.6.118-android15-8-gf4dc45704e54-abogki20260727-4k`；Image 39,262,720 字节；SHA-256 `913729fd15323980adb210bcb9f49e1fc949adc0d92433803e05934503fe9233`。

## DroidSpaces panic 保护

- 真机 minidump 已确认 panic 位于厂商永久模块 `oplus_bsp_sched_assist` 的 `update_ddl_hit_history()`：失效 task 的 pid 被用作 `ddl_sdata[PID_MAX_DEFAULT]` 下标，触发 `UBSAN: array index out of bounds`，随后因 `CONFIG_PANIC_ON_OOPS=y` 重启。
- 厂商源码修复已存在于 `op_mods` 提交 `a772844`，但当前工作区缺少完整 OEM `kernel/oplus_cpu` 源码，无法安全生成与已加载永久模块 ABI 匹配的 `.ko`。
- 已提交并部署 `op_mods` 的 `53674b4`：KernelSU `service.d/99-oplus-sched-ddl-guard.sh` 在每次启动等待节点出现并写入 `0`。本次设备实测脚本哈希匹配、权限 `0755`、当前 `sched_ddl_enabled=0`，未重启设备，避免无必要风险。
